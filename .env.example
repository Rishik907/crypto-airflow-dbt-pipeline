# Copy this file to .env and fill in real values. .env is git-ignored.

AIRFLOW_UID=50000

# Optional — sensible local-dev defaults are baked into docker-compose.yaml.
# Override these if this ever runs somewhere beyond your own laptop.
# FERNET_KEY=
# AIRFLOW_JWT_SECRET=
# AIRFLOW_JWT_ISSUER=

SNOWFLAKE_ACCOUNT=DJPBQEV-CK77560
SNOWFLAKE_USER=rishi907
SNOWFLAKE_PASSWORD=<>
SNOWFLAKE_ROLE=CRYPTO_ANALYTICS_ROLE
SNOWFLAKE_WAREHOUSE=SEC_PIPELINE_WH

# CoinGecko free tier works without a key; set this only if you have a Demo API key
COINGECKO_API_KEY=
